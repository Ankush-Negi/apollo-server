input userCreateInput {
    name: String!
    email: String!
    password: String!
}

input userUpdateInput {
    name: String
    email: String
    password: String
    id: String!
}

input getAllInput {
    skip: Int
    limit: Int
}

type Trainee {
    records : [User]
    count: Int
}